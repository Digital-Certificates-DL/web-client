export enum TemplateFieldEnum {
  name = 'name',
  credits = 'credits',
  exam = 'exam',
  course = 'course',
  level = 'level',
  qr = 'qr',
  points = 'points',
  date = 'date',
  serial_number = 'serial_number',
}
